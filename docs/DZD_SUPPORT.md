# دعم الدينار الجزائري (DZD) في SAFRGO 🇩🇿

## التحديثات الجديدة

تم إضافة دعم كامل للدينار الجزائري (DZD) كعملة أساسية في نظام SAFRGO!

## ✨ الميزات

### 1️⃣ الدينار الجزائري كخيار افتراضي
- عند إنشاء عرض جديد، DZD هو الخيار الأول في القائمة
- التنسيق: `15,000 دج`

### 2️⃣ دعم عملات متعددة
- 🇩🇿 دج (DZD) - الدينار الجزائري
- 🇺🇸 دولار (USD) - الدولار الأمريكي
- 🇸🇦 ر.س (SAR) - الريال السعودي
- 🇦🇪 د.إ (AED) - الدرهم الإماراتي
- 🇪🇺 يورو (EUR) - اليورو

### 3️⃣ دالة تنسيق ذكية
```typescript
formatPrice(15000, 'DZD')  // → "15,000 دج"
formatPrice(150, 'USD')     // → "$150"
formatPrice(5000, 'SAR')    // → "5,000 ر.س"
```

## 🎯 كيفية الاستخدام

### للوكالات السياحية
1. اذهب إلى **عروضي** → **إنشاء عرض جديد**
2. في قسم **التسعير والمدة**، اختر العملة من القائمة
3. الدينار الجزائري (دج) هو الخيار الافتراضي
4. أدخل السعر بالعملة المختارة
5. احفظ العرض

### مثال
```
عنوان العرض: رحلة إلى الصحراء الكبرى
السعر: 25000
العملة: دج (DZD)
المدة: 3 أيام / 2 ليالي
```

**النتيجة**: يظهر العرض للمسافرين بسعر `25,000 دج`

## 📊 أمثلة واقعية

### عرض سياحي محلي
- **الوجهة**: قسنطينة
- **السعر**: 15,000 دج
- **المدة**: يومان
- **العرض**: `15,000 دج`

### عرض عمرة
- **الوجهة**: مكة المكرمة
- **السعر الأصلي**: 180,000 دج
- **السعر بعد الخصم**: 150,000 دج
- **الوفر**: 30,000 دج

### عرض دولي
- **الوجهة**: دبي
- **السعر**: $500
- **أو**: 2,500 د.إ
- **أو**: 75,000 دج (يمكن للوكالة اختيار العملة)

## 🔧 للمطورين

### التحديثات التقنية

#### 1. دالة formatPrice جديدة
```typescript
// lib/utils.ts
export function formatPrice(price: number, currency: string = 'DZD'): string {
  const formattedAmount = price.toLocaleString('ar-DZ')
  
  switch (currency) {
    case 'DZD': return `${formattedAmount} دج`
    case 'USD': return `$${formattedAmount}`
    case 'SAR': return `${formattedAmount} ر.س`
    case 'AED': return `${formattedAmount} د.إ`
    case 'EUR': return `€${formattedAmount}`
    default: return `${formattedAmount} ${currency}`
  }
}
```

#### 2. تحديث قاعدة البيانات
```sql
-- scripts/013_update_currency_default.sql
ALTER TABLE public.offers 
ALTER COLUMN currency SET DEFAULT 'DZD';
```

#### 3. الملفات المحدثة
- ✅ `lib/utils.ts` - دالة formatPrice
- ✅ `components/agency/offer-form.tsx` - قائمة العملات
- ✅ `components/traveler/*` - جميع صفحات عرض الأسعار
- ✅ `components/admin/*` - لوحة التحكم
- ✅ `components/landing/*` - الصفحة الرئيسية

## 📱 العرض في التطبيق

### على بطاقة العرض
```
┌─────────────────────────┐
│ [صورة الوجهة]          │
│                         │
│ 15,000 دج ←            │
└─────────────────────────┘
  رحلة إلى تيمقاد
  قسنطينة • 3 أيام
```

### في صفحة التفاصيل
```
السعر: 25,000 دج
السعر الأصلي: 30,000 دج
──────────────────
وفر: 5,000 دج
```

## 🚀 الخطوات التالية المقترحة

1. ✅ دعم الدينار الجزائري - **تم**
2. 🔄 تحويل العملات الآلي (API)
3. 📊 تقارير بالعملات المختلفة
4. 💱 عرض الأسعار بعملات متعددة للعرض الواحد

## 📖 التوثيق الكامل

راجع [CURRENCY_SYSTEM.md](./CURRENCY_SYSTEM.md) للتفاصيل التقنية الكاملة.

---

**ملاحظة**: جميع العروض القديمة بالدولار (USD) ستستمر في العمل بشكل طبيعي. 
الدينار الجزائري هو فقط الخيار الافتراضي للعروض الجديدة.
